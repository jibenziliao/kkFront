kkFrontDemo模板项目
引入指令需要将常量中的配置导入到自己的项目中；
引入指令需要引入指令对应的样式文件；
kkFrontUtils模块中的一些指令依赖于第三方模块（utils目录下）；
更改kkFrontUtils模块名时，应一并更改utils目录下lodash.js中的模块命名。

此压缩包解压后，运行
npm install
bower install
再运行gulp即可启动项目